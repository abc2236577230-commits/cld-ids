# 实验数据审计报告（DATA AUDIT）

> 审计日期：2026-08-22　工具：`code/audit_data.py`（可复跑）
> 范围：`results/matrix_*.csv` 全部 700 行（2 数据集 × 5 场景 × 7 模型 × 10 seeds）

## 一、审计结论

**结构完整性全部通过，无数据级 bug。** 有 4 项发现需要在论文写作中显式处理（非错误，属指标性质与基线退化行为）。

## 二、逐项检查结果

| 检查 | 内容 | 结果 |
|---|---|---|
| A 结构 | 行数/重复键/种子完备性 | ✅ 700 行、0 重复、每格 seeds{0..9} 齐全 |
| B 配置一致性 | 各 shard 的 lr/window/rec_thr/warmup/cooldown/delta/start_after/use_cat 与规范值逐字段比对 | ✅ 基础配置组与 ft 组（warmup=2000, finetune=3）零漂移 |
| C 字段合法性 | onset 向量长度(S1:4,S2:3,S3:6,S4:4,Srare:3)、触发次数=触发列表长度且单调递增且在流内、f1/gmean∈[0,1]、恢复时间≥窗口下界 | ✅ 全部通过 |
| G 统计交叉验证 | 独立重算 70 个聚合单元格 vs stats_summary.md | ✅ 0 处不符；仅 1 个 run 无有效恢复值被正确排除（nslkdd/Srare drift seed4，恢复向量全 None） |

## 三、发现与处置（写作阶段必须落实）

### D1. 恢复时间指标地板效应（重要）
`recovery == 2000`（=滚动窗口长度，度量下界）的占比在大量格子中达到 100%：
- **S2_gradual 全部 7 模型 100% 饱和**——渐进 ramp 场景对在线学习天然容易，任何方法都能在一个窗口内"恢复"
- ARF 在所有场景 100% 饱和；drift_ft 在 S1/S2 达 93–100%
- 只有 plain / drift / periodic 等弱配置在 S1/S3/S4/Srare 上产生区分度

**处置**：(a) 论文明确恢复时间的定义与下界（首次"整窗错误率≤阈值"，最小值恒为窗口长）；(b) 主区分指标改用**错误 AUC**（不饱和）+ Srare/S1/S3 的 F1；(c) fig5 窗口惯性分解承担解释功能；(d) 相关叙述不得用"S2 恢复时间差异"作为证据。

### E. 极端异常值集中于 drift 模型（有利证据）
全部 BIG-REC/BIG-AUC 异常（如 unsw_full S3 seed1 rec=51699≈段尾、auc≈0.999；nslkdd seed4 多场景病态）均出自 **drift（重置无微调）**。这不是脏数据，而是 D1 机制发现的直接体现：梯度学习器重置后进入病态区。写作时作为案例引用。

### F. periodic 跨种子零方差（良性，需脚注披露）
8 个格子的 periodic overall_f1 十个种子完全相同（如 unsw_full/S1 = 0.9143）。已验证其恢复向量与 AUC 向量亦逐位相同：periodic 在攻击段坍缩为 ~50% 错误的抛硬币式预测（auc=0.4999），行为只由固定分段几何决定，对采样行与初始化均不敏感。**非代码 bug**（其他模型同种子流程下方差正常）。处置：正文脚注说明该基线退化为内容无关预测，统计检验按符号秩处理常数基线仍有效。

### 杂项
- `steady_gmean` 列 700/700 恒为 NA（死列）：runner 未输出该指标。不影响任何结论，保留原样即可，论文勿引用该列。
- nslkdd/Srare drift seed4 从未恢复（None;None;None）：真实失败案例，聚合中被正确排除，可在正文作个案。

## 四、放行声明

数据层面可以开始写作。写作红线：
1. 恢复时间只在未饱和场景引用，饱和场景改引 AUC；
2. periodic 零方差需脚注；
3. ARF 最强 + 最慢的双面事实必须同时呈现（见 README 关键结果速记）。
